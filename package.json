{
  "name": "changethis",
  "version": "0.1.0",
  "private": true,
  "license": "EUPL-1.2",
  "description": "Client website feedback that turns into clean GitHub Issues.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build --workspace @changethis/shared && npm run dev --workspace @changethis/frontend",
    "build": "npm run build --workspace @changethis/shared && npm run build --workspace @changethis/widget && npm run build --workspace @changethis/frontend",
    "build:prod": "npm run env:check && npm run build",
    "color:check": "node scripts/check-color-policy.mjs",
    "env:check": "node scripts/check-env.mjs",
    "lint": "npm run lint --workspace @changethis/shared && npm run build --workspace @changethis/shared && npm run lint --workspace @changethis/widget && npm run lint --workspace @changethis/frontend",
    "prod:check": "npm run env:check && npm run typecheck",
    "security:audit": "npm audit --audit-level=high",
    "smoke:widget": "node scripts/smoke-widget-public.mjs",
    "test": "npm run test --workspace @changethis/shared",
    "typecheck": "npm run build --workspace @changethis/shared && npm run typecheck --workspace @changethis/widget && npm run typecheck --workspace @changethis/frontend",
    "widget:build": "npm run build --workspace @changethis/shared && npm run build --workspace @changethis/widget",
    "widget:size": "node scripts/check-widget-bundle-size.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
